# SmokeyText.js, jQuery plugin for smoking text

This library requires lettering.js and jQuery and uses the animation libraries to make your text go up in smoke.

## Usage
Quite simply really:

``` javascript
$('textspan').smoke({duration: 5});